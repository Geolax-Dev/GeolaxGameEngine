#include "BgfxCapturer.h"

void GGE::BgfxCapturer::beginVideoCapture(uint32_t _width, uint32_t _height, uint32_t _pitch, bgfx::TextureFormat::Enum _format, bool _yflip)
{
    //GGE_ASSERT(false);
}

void GGE::BgfxCapturer::endVideoCapture()
{
    //GGE_ASSERT(false);
}

void GGE::BgfxCapturer::pushVideoCaptureFrame(const void* _data, uint32_t _size)
{
    //GGE_ASSERT(false);
}

void GGE::BgfxCapturer::makeScreenshot(const char* filePath, uint32_t width, uint32_t height, uint32_t pitch, const void* data, uint32_t size, bool yflip)
{
    //GGE_ASSERT(false);
}
